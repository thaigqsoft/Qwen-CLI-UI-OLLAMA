# Backend server port
PORT=5008

# Frontend port (Vite)
VITE_PORT=5009

# Agent CLI integration (Codex or Qwen)
# Option 1: Simple binary + args mode (defaults compatible with Qwen)
#   AGENT_BIN=qwen
#   AGENT_SUBCOMMAND=exec
#   AGENT_MODEL_FLAG=-m
#   AGENT_IMAGE_FLAG=-i
#   AGENT_SKIP_PERMISSIONS_FLAG=--dangerously-bypass-approvals-and-sandbox
#   AGENT_EXTRA_ARGS=--skip-git-repo-check
#
# Option 2: Full command template (recommended for Qwen)
#   Provide a shell command template. Placeholders: {prompt_file} {cwd} {model} {images} {skip_permissions_flag}
#   - {skip_permissions_flag} is replaced with AGENT_SKIP_PERMISSIONS_FLAG only when UI "Permission Settings" is enabled
#   Example (edit to match your local Qwen CLI):
#   AGENT_SKIP_PERMISSIONS_FLAG=--yes
#   AGENT_CMD_TEMPLATE=qwen chat {skip_permissions_flag} --model {model} --input-file {prompt_file}
#
# Interactive shell (WS /shell) command and resume flag (optional):
#   AGENT_SHELL_CMD=qwen
#   AGENT_RESUME_FLAG=--resume

# Backward compatibility (Qwen):
# QWEN_PATH=qwen

# Ollama Configuration
# Set OLLAMA_URL to connect to a local or remote Ollama server
# Default: http://localhost:11434 (local Ollama)
# Example for remote server: http://192.168.1.100:11434
OLLAMA_URL=http://localhost:11434

# Database file path (optional)
# DATABASE_PATH=./data/database.sqlite

# JWT Secret (required for production)
# Generate a secure random string for production use
JWT_SECRET=your-secret-key-here

# Node environment
NODE_ENV=development
